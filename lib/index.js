// Generated by CoffeeScript 1.6.3
(function() {
  var callback, colors, dogFile, exec;

  exec = require('child_process').exec;

  colors = require('colors');

  dogFile = "" + __dirname + "/../bin/dog";

  callback = function(err, stdout, stderr) {
    if (err != null) {
      console.log(err.toString().red);
      console.log('Message:'.grey);
      console.log(stdout.toString().red);
    } else {
      console.log(stdout.toString().green);
    }
    return process.exit();
  };

  task('watch', 'real-time compile coffee scripts to javascript files', function(options) {
    return exec("" + dogFile + " watch", {
      timeout: 100
    }, callback);
  });

  task('unwatch', 'stop compile coffee scripts to javascript files', function(options) {
    return exec("" + dogFile + " unwatch", callback);
  });

  task('generate', "once compile coffee scripts to javascript files", function(options) {
    return exec("" + dogFile + " generate", callback);
  });

}).call(this);
